# Dev Toolkit —— UI 风格描述

## 风格定位

「深色科技毛玻璃 + 多主题」的开发者工具箱：苹果式克制质感（半透明毛玻璃、柔和分层阴影、弹性动效）混一点科幻氛围（环境光斑、细线圆环、网格、噪点）。不拟物、不用重边框、不堆高饱和色，一切轻、透、静。

## 核心视觉语言

- **毛玻璃**：所有面板/卡片/侧边栏/浮层都是半透明模糊面（blur + saturate），透出背后的环境背景；每张卡片顶部有一条 1px 高光线，像被光照亮的玻璃边缘。深浅主题自适应，弱化支持时回退实色。
- **环境背景**：页面底色之上浮着 3 个大号柔和光斑（强模糊径向渐变，分别飘在左上/右上/底部，缓慢漂移呼吸）、两个细线大圆环、从中心淡出的网格线、低透明度噪点——画面有"空间纵深"但不抢内容。
- **阴影**：小控件用轻投影，浮层/弹层用大而柔的阴影（深色主题下阴影还带一点主题色的氛围光）。
- **色彩**：每个主题一个强调色（浅色蓝、深色暖橙、陶土橙、鼠尾草绿）+ 一套中性灰阶（主文字/次级/三级）。强调色只用于：选中态、聚焦态、主按钮、关键数字、高亮底。大量使用「强调色的低透明度淡底」（如 7-16% 透明度）做选中/强调背景，而不是直接铺满强调色。
- **圆角语言**：小控件 12px、卡片/面板 16px、浮层/徽标/按钮 全圆——层级越高越圆。
- **字体气质**：正文系统无衬线，标题收紧字距（-0.02em 左右），小标签大写拉宽字距，数字用等宽数字对齐；代码与数字输入用等宽字体。

## 组件气质

- **控件全部自绘**、无原生痕迹：胶囊按钮、圆角输入框、自绘下拉、分段选择器（选中段"浮起"）、圆角开关。
- **聚焦态**：边框变为强调色 + 外圈一圈淡色光晕，整体微微上浮——温柔但清晰。
- **信息层级**：卡片 = 玻璃面；卡片内部用 `inset 顶高光线` + 左上角一团 accent 色径向光晕增加质感；强调结果（如金额）用「accent 淡底渐变卡」突出。

## 动效气质

- **只动 transform/opacity**，全部是短距离、克制、可打断的：悬停 1px 上浮、按下缩到 97%、卡片悬停抬升几像素 + 顶部一条强调色流光扫过、页面/工具切换是快速上浮淡入。
- **错峰入场**：标题、卡片依次延迟入场，像排队进场的观众。
- **主题切换**：短暂纯色遮罩淡出，柔化明暗突变。
- 全程尊重系统减少动态/透明偏好。

## 首页气质

更活泼的展示页：大号超粗标题（局部用蓝紫红渐变文字），绿色呼吸圆点徽标，工具卡片在悬停时弹簧抬升、光标跟随亮点、边框 conic 流光旋转、渐变图标方块微放大旋转、右上箭头变主题色并旋转 45°——精致而克制，不与工作台脱节。

## 一句话总结

**深色科技玻璃 + 柔和环境光 + 克制的苹果式动效 + 强调色淡底体系**——像一块悬浮在深色太空里的毛玻璃操作台。
